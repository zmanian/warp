# A reusable workflow that encapsulates all of the logic surrounding building
# all bundles for a single release channel, either new releases (when run on the
# `master` branch) or updated release candidates (when run within a `*_release`
# branch).
#
# This can either be triggered:
# * Indirectly, using workflow_call, for creating real releases.
# * Directly, using workflow_dispatch, for testing. By default, this will not create GitHub or Sentry releases, or upload to GCS.

name: Create Release
on:
  workflow_call:
    inputs:
      channel:
        description: The channel to create a release or release candidate for.
        required: true
        type: string
      build_linux:
        description: Build Linux artifacts.
        type: boolean
        default: true
      build_windows:
        description: Build Windows artifacts.
        type: boolean
        default: true
      build_macos:
        description: Build macOS artifacts.
        type: boolean
        default: true
      build_web:
        description: Build web artifacts.
        type: boolean
        default: true
      should_publish:
        description: Publish this release
        type: boolean
        default: true
  workflow_dispatch:
    inputs:
      channel:
        type: string
        required: true
      build_linux:
        description: Build Linux artifacts.
        type: boolean
        default: true
      build_windows:
        description: Build Windows artifacts.
        type: boolean
        default: true
      build_macos:
        description: Build macOS artifacts.
        type: boolean
        default: true
      build_web:
        description: Build web artifacts.
        type: boolean
        default: true

env:
  CARGO_TERM_COLOR: always
  CONFIG_FILE: ".github/workflows/release_configurations.json"

jobs:
  # Perform all once-per-release steps.  Dependent jobs will build release
  # assets and upload them as appropriate.
  prepare_release:
    name: Prepare release
    runs-on: ubuntu-latest
    outputs:
      release_branch: ${{ steps.create_branch_and_tag.outputs.branch }}
      release_tag: ${{ steps.create_branch_and_tag.outputs.tag }}
      should_publish: ${{ steps.set_publish.outputs.should_publish }}
    steps:
      - name: Checkout sources
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}
      - name: Set publish flag based on the workflow trigger
        id: set_publish
        run: |
          # The should_publish input is only set on the workflow_call trigger.
          # This means it's not possible to trigger publishing via workflow_dispatch, and
          # publishing is enabled by default when called for a new release or RC.
          if [[ "${{ inputs.should_publish }}" == "true" ]]; then
            echo "should_publish=true" >> $GITHUB_OUTPUT
          else
            echo "should_publish=false" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - name: Update branch and tag
        id: create_branch_and_tag
        run: |
          if [[ "${{ steps.set_publish.outputs.should_publish }}" == "true" ]]; then
            GIT_BRANCH_NAME="${GITHUB_REF#refs/heads/}"
            source ./script/create_release_tag_and_branch --channel $CHANNEL --branch-name "$GIT_BRANCH_NAME"
            echo "branch=$(git branch --show-current)" >> $GITHUB_OUTPUT
            echo "tag=$tag" >> $GITHUB_OUTPUT
          else
            echo "branch=$(git branch --show-current)" >> $GITHUB_OUTPUT
            # Use a fake version tag that will pass package validation.
            echo "tag=v0.$GITHUB_SHA" >> $GITHUB_OUTPUT
          fi
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          # See https://github.com/orgs/community/discussions/151442 for why we need to use
          # a PAT here.
          GITHUB_TOKEN: ${{ secrets.CREATE_RELEASE_TAG_PUSH_PAT }}

      - name: Create GitHub release
        if: ${{ steps.set_publish.outputs.should_publish == 'true' }}
        id: create_release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          name: ${{ steps.get-config.outputs.release_base_name }} ${{ steps.create_branch_and_tag.outputs.tag }}
          tag_name: ${{ steps.create_branch_and_tag.outputs.tag }}
          body: ${{ steps.get-config.outputs.release_body_text }}
          draft: false
          prerelease: ${{ steps.get-config.outputs.is_prerelease }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Sentry release
        if: ${{ steps.set_publish.outputs.should_publish == 'true' }}
        uses: getsentry/action-release@ff07929a6537bac57790c3451cf4d364aca38528 # v3.7.0
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          SENTRY_ORG: warpdotdev
          SENTRY_PROJECT: ${{ steps.get-config.outputs.sentry_project }}
        with:
          environment: ${{ steps.get-config.outputs.sentry_environment }}
          version: ${{ steps.create_branch_and_tag.outputs.tag }}

  release_macos_single_arch:
    name: Build Release (macOS ${{ matrix.dmg_name_suffix }})
    runs-on: macos-26-xlarge
    needs: prepare_release
    if: ${{ inputs.build_macos != false }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
            dmg_name_suffix: arm64
          - arch: x86_64
            dmg_name_suffix: x86_64
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Ensure rust target is installed
        run: rustup target add ${{ matrix.arch }}-apple-darwin
        shell: bash

      # Install go toolchain, which is needed for bundling a pprof binary.
      - name: Setup Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: stable
          cache: false

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Install cargo-bundle
        run: script/install_cargo_bundle

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Build ${{ matrix.arch }} bundle
        id: bundle_app
        run: |
          script/bundle --read-passwords-from-env --channel $CHANNEL --arch ${{ matrix.arch }} --dmg-name-suffix "${{ matrix.dmg_name_suffix }}"
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          WARP_DEVELOPER_ID_CERT: ${{ secrets.WARP_DEVELOPER_ID_CERT }}
          WARP_DEVELOPER_ID_CERT_PASSWORD: ${{ secrets.WARP_DEVELOPER_ID_CERT_PASSWORD }}
          WARP_CODESIGN_KEYCHAIN_PASSWORD: ${{ secrets.WARP_CODESIGN_KEYCHAIN_PASSWORD }}
          WARP_NOTARIZATION_APPLE_ID: ${{ secrets.WARP_NOTARIZATION_APPLE_ID }}
          WARP_NOTARIZATION_PASSWORD: ${{ secrets.WARP_NOTARIZATION_PASSWORD }}

      - name: Archive ${{ matrix.arch }} macOS settings schema
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: settings-schema-macos-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          path: ${{ steps.bundle_app.outputs.settings_schema_path }}

      - name: Add DMG to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_app.outputs.dmg_path }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload DMG to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_app.outputs.dmg_path }}
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload DMG as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-macos-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          path: ${{ steps.bundle_app.outputs.dmg_path }}

      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      - name: Upload debug symbols from frameworks to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        shell: bash
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_app.outputs.frameworks_dir }}

      # Create a tar archive instead of simply using upload-artifact so we can
      # preserve permissions and symlinks.  If we don't preserve symlinks, we'll
      # break the macOS framework directory structure.
      - name: Create tar archive with build artifacts
        run: |
          tar czfv build-artifacts.tar.gz \
            ${{ steps.bundle_app.outputs.binary_path }} \
            ${{ steps.bundle_app.outputs.dock_tile_plugin_dir }} \
            ${{ steps.bundle_app.outputs.dsym_path }} \
            ${{ steps.bundle_app.outputs.dsym_realpath }} \
            ${{ steps.bundle_app.outputs.frameworks_dir }}

      - name: Archive ${{ matrix.arch }} build artifacts for universal build
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          # We compress the tar archive, so no need to re-compress.
          compression-level: 0
          path: build-artifacts.tar.gz

  release_macos_universal:
    name: Build Release (macOS Universal)
    runs-on: macos-26-xlarge
    needs: [prepare_release, release_macos_single_arch]
    if: ${{ inputs.build_macos != false }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      # Install go toolchain, which is needed for bundling a pprof binary.
      - name: Setup Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: stable
          cache: false

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Download aarch64 build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-aarch64-${{ steps.get-config.outputs.channel }}

      - name: Extract aarch64 build artifacts
        run: tar xvfz build-artifacts.tar.gz
        shell: bash

      - name: Download x86_64 build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-x86_64-${{ steps.get-config.outputs.channel }}

      - name: Extract x86_64 build artifacts
        run: tar xvfz build-artifacts.tar.gz
        shell: bash

      - name: Download canonical aarch64 macOS settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-macos-aarch64-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-macos

      - name: Download x86_64 macOS settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-macos-x86_64-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-macos-x86_64

      # These two schemas are expected to be byte-identical: no setting is currently scoped to
      # a specific macOS architecture. A mismatch here means that assumption no longer holds,
      # and this step needs to become architecture-aware rather than simply failing the release.
      - name: Verify macOS settings schemas match
        run: cmp .release-artifacts/settings-schema-macos/settings_schema.json .release-artifacts/settings-schema-macos-x86_64/settings_schema.json
        shell: bash

      - name: Install cargo-bundle
        run: script/install_cargo_bundle

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Bundle universal app
        id: bundle_app
        run: |
          script/bundle --skip-build --read-passwords-from-env --channel $CHANNEL
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          WARP_DEVELOPER_ID_CERT: ${{ secrets.WARP_DEVELOPER_ID_CERT }}
          WARP_DEVELOPER_ID_CERT_PASSWORD: ${{ secrets.WARP_DEVELOPER_ID_CERT_PASSWORD }}
          WARP_CODESIGN_KEYCHAIN_PASSWORD: ${{ secrets.WARP_CODESIGN_KEYCHAIN_PASSWORD }}
          WARP_NOTARIZATION_APPLE_ID: ${{ secrets.WARP_NOTARIZATION_APPLE_ID }}
          WARP_NOTARIZATION_PASSWORD: ${{ secrets.WARP_NOTARIZATION_PASSWORD }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-macos/settings_schema.json

      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      - name: Upload debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        shell: bash
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_app.outputs.dsym_folder_path }}

      - name: Add DMG to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        id: upload_github_assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        # Continue if the upload fails; the next step will retry it.
        continue-on-error: true
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_app.outputs.dmg_path }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Add DMG to GitHub release assets (retry 1/2)
        id: upload_github_assets_retry
        # If the first attempt failed, try again.
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' && steps.upload_github_assets.outcome == 'failure' }}
        # Continue if the upload fails; the next step will retry it.
        continue-on-error: true
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_app.outputs.dmg_path }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Add DMG to GitHub release assets (retry 2/2)
        id: upload_github_assets_retry2
        # If the first attempt failed, try again.
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' && steps.upload_github_assets_retry.outcome == 'failure' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_app.outputs.dmg_path }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload DMG to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_app.outputs.dmg_path }}
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
          # IMPORTANT: both the key and value of the cache-control header must be all lowercase;
          # uppercase values will not be respected by Cloud CDN and using "Cache-Control" will be
          # ignored by the upload-cloud-storage action.
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload DMG as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-macos-universal-${{ steps.get-config.outputs.channel }}
          path: ${{ steps.bundle_app.outputs.dmg_path }}

  build_macos_cli_binaries:
    name: Build Release Binary (macOS CLI ${{ matrix.arch }})
    runs-on: macos-26-xlarge
    needs: prepare_release
    if: ${{ inputs.build_macos != false }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
          - arch: x86_64
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Ensure rust target is installed
        run: rustup target add ${{ matrix.arch }}-apple-darwin
        shell: bash

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      # Only the binary matters here; this job's own bundled resources are
      # discarded, so skip signing/notarization and the (otherwise mandatory)
      # schema, both of which belong to the packaging step below.
      - name: Build ${{ matrix.arch }} binary
        id: build_cli
        run: |
          script/bundle --channel $CHANNEL --arch ${{ matrix.arch }} --artifact cli --nosign
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          NO_LICENSES: "1"
          SKIP_SETTINGS_SCHEMA: "1"

      # binary_path is already copied out of the per-target-triple Cargo output
      # directory; --skip-build (used by release_macos_cli below) re-derives
      # that copy from the triple directory, so preserve it here instead. The
      # dSYM Cargo produces there is itself a symlink into target/.../deps, so
      # (as with the app archive above) also include its resolved target;
      # otherwise the link dangles once extracted into a fresh job.
      - name: Create tar archive with build artifacts
        run: |
          BINARY_PATH="${{ steps.build_cli.outputs.binary_path }}"
          PROFILE_DIR="${BINARY_PATH#target/}"
          PROFILE_DIR="${PROFILE_DIR%/bundle/osx/*}"
          RAW_BINARY_PATH="target/${{ steps.build_cli.outputs.rust_target }}/$PROFILE_DIR/$(basename "$BINARY_PATH")"
          RAW_DSYM_PATH="$RAW_BINARY_PATH.dSYM"
          TAR_PATHS=("$RAW_BINARY_PATH")
          if [[ -e "$RAW_DSYM_PATH" ]]; then
            TAR_PATHS+=("$RAW_DSYM_PATH")
            if [[ -L "$RAW_DSYM_PATH" ]]; then
              REAL_DSYM_PATH="$(python3 -c 'import os,sys; print(os.path.relpath(sys.argv[1]))' "$(realpath "$RAW_DSYM_PATH")")"
              TAR_PATHS+=("$REAL_DSYM_PATH")
            fi
          fi
          tar czfv build-artifacts.tar.gz "${TAR_PATHS[@]}"
        shell: bash

      - name: Archive ${{ matrix.arch }} CLI build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos-cli-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          # We compress the tar archive, so no need to re-compress.
          compression-level: 0
          path: build-artifacts.tar.gz

  # Packages the CLI binary build_macos_cli_binaries built separately (in
  # parallel), then adds the canonical schema and signs/notarizes.
  release_macos_cli:
    name: Build Release (macOS CLI ${{ matrix.arch }})
    runs-on: macos-26-xlarge
    needs: [prepare_release, release_macos_single_arch, build_macos_cli_binaries]
    if: ${{ inputs.build_macos != false }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
          - arch: x86_64
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Download ${{ matrix.arch }} CLI build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-cli-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}

      - name: Extract CLI build artifacts
        run: tar xvfz build-artifacts.tar.gz
        shell: bash

      - name: Download canonical macOS settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-macos-aarch64-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-macos

      - name: Bundle ${{ matrix.arch }} binary
        id: bundle_cli
        run: |
          script/bundle --skip-build --read-passwords-from-env --channel $CHANNEL --arch ${{ matrix.arch }} --artifact cli
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          WARP_DEVELOPER_ID_CERT: ${{ secrets.WARP_DEVELOPER_ID_CERT }}
          WARP_DEVELOPER_ID_CERT_PASSWORD: ${{ secrets.WARP_DEVELOPER_ID_CERT_PASSWORD }}
          WARP_CODESIGN_KEYCHAIN_PASSWORD: ${{ secrets.WARP_CODESIGN_KEYCHAIN_PASSWORD }}
          WARP_NOTARIZATION_APPLE_ID: ${{ secrets.WARP_NOTARIZATION_APPLE_ID }}
          WARP_NOTARIZATION_PASSWORD: ${{ secrets.WARP_NOTARIZATION_PASSWORD }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-macos/settings_schema.json

      - name: Package CLI binary
        run: |
          mv ${{ steps.bundle_cli.outputs.binary_path }} oz-${{ steps.get-config.outputs.channel }}
          tar czf oz-${{ steps.get-config.outputs.channel }}-macos-${{ matrix.arch }}.tar.gz oz-${{ steps.get-config.outputs.channel }} -C "$(dirname "${{ steps.bundle_cli.outputs.bundled_resources_dir }}")" resources

      - name: Add CLI to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: oz-${{ steps.get-config.outputs.channel }}-macos-${{ matrix.arch }}.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload CLI to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: oz-${{ steps.get-config.outputs.channel }}-macos-${{ matrix.arch }}.tar.gz
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}/cli/macos/${{ matrix.arch }}
          process_gcloudignore: false
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload CLI as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-macos-cli-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          path: oz-${{ steps.get-config.outputs.channel }}-macos-${{ matrix.arch }}.tar.gz

      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      - name: Upload debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        shell: bash
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_cli.outputs.dsym_path }}

  build_macos_tui_binaries:
    name: Build Release Binary (macOS TUI ${{ matrix.arch }})
    runs-on: macos-26-xlarge
    needs: prepare_release
    if: ${{ inputs.build_macos != false }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
          - arch: x86_64
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Ensure rust target is installed
        run: rustup target add ${{ matrix.arch }}-apple-darwin
        shell: bash

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      # Only the binary matters here; this job's own bundled resources are
      # discarded, so skip signing/notarization and the (otherwise mandatory)
      # schema, both of which belong to the packaging step below.
      - name: Build ${{ matrix.arch }} binary
        id: build_tui
        run: |
          script/bundle --channel $CHANNEL --arch ${{ matrix.arch }} --artifact tui --nosign
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          NO_LICENSES: "1"
          SKIP_SETTINGS_SCHEMA: "1"

      # binary_path is already copied out of the per-target-triple Cargo output
      # directory; --skip-build (used by release_macos_tui below) re-derives
      # that copy from the triple directory, so preserve it here instead. The
      # dSYM Cargo produces there is itself a symlink into target/.../deps, so
      # (as with the app archive above) also include its resolved target;
      # otherwise the link dangles once extracted into a fresh job.
      - name: Create tar archive with build artifacts
        run: |
          BINARY_PATH="${{ steps.build_tui.outputs.binary_path }}"
          PROFILE_DIR="${BINARY_PATH#target/}"
          PROFILE_DIR="${PROFILE_DIR%/bundle/osx/*}"
          RAW_BINARY_PATH="target/${{ steps.build_tui.outputs.rust_target }}/$PROFILE_DIR/$(basename "$BINARY_PATH")"
          RAW_DSYM_PATH="$RAW_BINARY_PATH.dSYM"
          TAR_PATHS=("$RAW_BINARY_PATH")
          if [[ -e "$RAW_DSYM_PATH" ]]; then
            TAR_PATHS+=("$RAW_DSYM_PATH")
            if [[ -L "$RAW_DSYM_PATH" ]]; then
              REAL_DSYM_PATH="$(python3 -c 'import os,sys; print(os.path.relpath(sys.argv[1]))' "$(realpath "$RAW_DSYM_PATH")")"
              TAR_PATHS+=("$REAL_DSYM_PATH")
            fi
          fi
          tar czfv build-artifacts.tar.gz "${TAR_PATHS[@]}"
        shell: bash

      - name: Archive ${{ matrix.arch }} TUI build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos-tui-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          # We compress the tar archive, so no need to re-compress.
          compression-level: 0
          path: build-artifacts.tar.gz

  # Packages the TUI binary build_macos_tui_binaries built separately (in
  # parallel), then adds the canonical schema and signs/notarizes.
  release_macos_tui:
    name: Build Release (macOS TUI ${{ matrix.arch }})
    runs-on: macos-26-xlarge
    needs: [prepare_release, release_macos_single_arch, build_macos_tui_binaries]
    if: ${{ inputs.build_macos != false }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
          - arch: x86_64
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Download ${{ matrix.arch }} TUI build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-tui-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}

      - name: Extract TUI build artifacts
        run: tar xvfz build-artifacts.tar.gz
        shell: bash

      - name: Download canonical macOS settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-macos-aarch64-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-macos

      - name: Bundle ${{ matrix.arch }} binary
        id: bundle_tui
        run: |
          script/bundle --skip-build --read-passwords-from-env --channel $CHANNEL --arch ${{ matrix.arch }} --artifact tui
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          WARP_DEVELOPER_ID_CERT: ${{ secrets.WARP_DEVELOPER_ID_CERT }}
          WARP_DEVELOPER_ID_CERT_PASSWORD: ${{ secrets.WARP_DEVELOPER_ID_CERT_PASSWORD }}
          WARP_CODESIGN_KEYCHAIN_PASSWORD: ${{ secrets.WARP_CODESIGN_KEYCHAIN_PASSWORD }}
          WARP_NOTARIZATION_APPLE_ID: ${{ secrets.WARP_NOTARIZATION_APPLE_ID }}
          WARP_NOTARIZATION_PASSWORD: ${{ secrets.WARP_NOTARIZATION_PASSWORD }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-macos/settings_schema.json

      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      - name: Verify and upload TUI debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          binary_path="${{ steps.bundle_tui.outputs.binary_path }}"
          dsym_path="${{ steps.bundle_tui.outputs.dsym_path }}"
          test -n "$dsym_path"
          test -d "$dsym_path"

          binary_uuid="$(dwarfdump --uuid "$binary_path" | awk '{print $2}')"
          dsym_uuid="$(dwarfdump --uuid "$dsym_path" | awk '{print $2}')"
          test -n "$binary_uuid"
          test "$binary_uuid" = "$dsym_uuid"

          dsym_dwarf_path="$(find "$dsym_path/Contents/Resources/DWARF" -type f -print -quit)"
          test -n "$dsym_dwarf_path"
          sentry-cli debug-files check "$dsym_dwarf_path"
          sentry-cli upload-dif "$dsym_path"
        shell: bash

      - name: Package TUI binary
        run: |
          mv ${{ steps.bundle_tui.outputs.binary_path }} warp-tui-${{ steps.get-config.outputs.channel }}
          tar czf warp-tui-${{ steps.get-config.outputs.channel }}-macos-${{ matrix.arch }}.tar.gz warp-tui-${{ steps.get-config.outputs.channel }} -C "$(dirname "${{ steps.bundle_tui.outputs.bundled_resources_dir }}")" resources

      - name: Add TUI to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: warp-tui-${{ steps.get-config.outputs.channel }}-macos-${{ matrix.arch }}.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload TUI to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: warp-tui-${{ steps.get-config.outputs.channel }}-macos-${{ matrix.arch }}.tar.gz
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}/tui/macos/${{ matrix.arch }}
          process_gcloudignore: false
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload TUI as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: release-macos-tui-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          path: warp-tui-${{ steps.get-config.outputs.channel }}-macos-${{ matrix.arch }}.tar.gz

  build_linux_tui_binaries:
    name: Build Release Binary (Linux TUI ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: prepare_release
    if: ${{ inputs.build_linux != false }}
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: namespace-profile-ubuntu-20-04
          - arch: aarch64
            runner: namespace-profile-ubuntu-20-04-arm
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: linux
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      # Install gcc 10, as gcc 9.5 has a bug with memcmp that is incompatible
      # with aws-lc-rs (see https://gcc.gnu.org/bugzilla/show_bug.cgi?id=95189).
      - name: Install and configure gcc-10
        run: |
          sudo apt update
          sudo apt install -y gcc-10 g++-10
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          gcc --version
          g++ --version

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      # Remove cached _bundle_ output that can be shared across different _jobs_.  This won't
      # remove any incremental build artifacts, only the final bundled output.
      - name: Clean stale bundle output
        run: |
          if [[ -d target ]]; then
            find target -path '*/bundle/linux' -type d -prune -exec rm -rf {} +
          fi
        shell: bash

      # Only the binary matters here; this job's own bundled resources are
      # discarded, so skip the (otherwise mandatory) schema and third-party
      # license generation, both of which belong to the packaging step below.
      - name: Build TUI binary
        id: build_tui
        run: script/bundle --channel "$CHANNEL" --artifact tui --arch "${{ matrix.arch }}"
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          NO_LICENSES: "1"
          SKIP_SETTINGS_SCHEMA: "1"

      # executable_path is the copy already staged into bundle/linux;
      # --skip-build (used by release_linux_tui below) instead re-copies from
      # the raw target/<profile>/<bin> Cargo output, which debug_executable_path
      # (that raw path plus ".debug") lets us recover without duplicating the
      # channel/profile-naming logic that lives in script/linux/bundle.
      - name: Create tar archive with build artifacts
        run: |
          DEBUG_EXECUTABLE_PATH="$(realpath --relative-to=$(pwd) "$DEBUG_EXECUTABLE_PATH")"
          RAW_EXECUTABLE_PATH="${DEBUG_EXECUTABLE_PATH%.debug}"
          tar czvf build-artifacts.tar.gz "$RAW_EXECUTABLE_PATH"
        shell: bash
        env:
          DEBUG_EXECUTABLE_PATH: ${{ steps.build_tui.outputs.debug_executable_path }}

      - name: Archive ${{ matrix.arch }} TUI build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linux-tui-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          path: build-artifacts.tar.gz

  # Packages the TUI binary build_linux_tui_binaries built separately (in
  # parallel), then adds the canonical schema.
  release_linux_tui:
    name: Build Release (Linux TUI ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: [prepare_release, release_linux_x86, build_linux_tui_binaries]
    if: ${{ inputs.build_linux != false }}
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: namespace-profile-ubuntu-20-04
          - arch: aarch64
            runner: namespace-profile-ubuntu-20-04-arm
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: linux
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      # Install gcc 10, as gcc 9.5 has a bug with memcmp that is incompatible
      # with aws-lc-rs (see https://gcc.gnu.org/bugzilla/show_bug.cgi?id=95189).
      - name: Install and configure gcc-10
        run: |
          sudo apt update
          sudo apt install -y gcc-10 g++-10
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          gcc --version
          g++ --version

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Download ${{ matrix.arch }} TUI build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: linux-tui-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}

      - name: Extract TUI build artifacts
        run: tar xzvf build-artifacts.tar.gz
        shell: bash

      - name: Download canonical Linux settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-linux-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-linux

      # Remove cached _bundle_ output that can be shared across different _jobs_.  This won't
      # remove any incremental build artifacts, only the final bundled output.
      - name: Clean stale bundle output
        run: |
          if [[ -d target ]]; then
            find target -path '*/bundle/linux' -type d -prune -exec rm -rf {} +
          fi
        shell: bash

      - name: Bundle TUI
        id: bundle_tui
        run: script/bundle --channel "$CHANNEL" --artifact tui --arch "${{ matrix.arch }}" --skip-build
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-linux/settings_schema.json

      - name: Package and validate TUI tarball
        run: |
          archive="warp-tui-${CHANNEL}-linux-${{ matrix.arch }}.tar.gz"
          bundle_dir="$(dirname "${{ steps.bundle_tui.outputs.executable_path }}")"
          tar czf "$archive" -C "$bundle_dir" "warp-tui-${CHANNEL}" resources

          test -x "$bundle_dir/warp-tui-${CHANNEL}"
          test -s "$bundle_dir/resources/settings_schema.json"
          test -s "$bundle_dir/resources/THIRD_PARTY_LICENSES.txt"
          grep -q "\"warp_version\": \"${GIT_RELEASE_TAG}\"" \
            "$bundle_dir/resources/bundled/metadata/version.json"

          archive_listing="${RUNNER_TEMP}/warp-tui-${CHANNEL}-linux-${{ matrix.arch }}-contents.txt"
          tar tzf "$archive" > "$archive_listing"
          grep -Fqx "warp-tui-${CHANNEL}" "$archive_listing"
          grep -Fqx "resources/settings_schema.json" "$archive_listing"
          grep -Fqx "resources/THIRD_PARTY_LICENSES.txt" "$archive_listing"
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}

      - name: Add TUI to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: warp-tui-${{ steps.get-config.outputs.channel }}-linux-${{ matrix.arch }}.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload TUI to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: warp-tui-${{ steps.get-config.outputs.channel }}-linux-${{ matrix.arch }}.tar.gz
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}/tui/linux/${{ matrix.arch }}
          process_gcloudignore: false
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload TUI as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: release-linux-tui-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          path: warp-tui-${{ steps.get-config.outputs.channel }}-linux-${{ matrix.arch }}.tar.gz

  release_linux_x86:
    name: Build Release (Linux x86_64)
    runs-on: namespace-profile-ubuntu-20-04
    needs: prepare_release
    if: ${{ inputs.build_linux != false }}
    timeout-minutes: 90
    env:
      # Automatically extract AppImages before running them instead of mounting
      # them with FUSE, which isn't available on GitHub runners (and this is
      # easier and less error-prone than trying to install it).
      APPIMAGE_EXTRACT_AND_RUN: "1"
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: linux
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      # Install gcc 10, as gcc 9.5 has a bug with memcmp that is incompatible
      # with aws-lc-rs (see https://gcc.gnu.org/bugzilla/show_bug.cgi?id=95189).
      - name: Install and configure gcc-10
        run: |
          sudo apt update
          sudo apt install -y gcc-10 g++-10
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          gcc --version
          g++ --version

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Install linuxdeploy
        run: script/linux/install_linuxdeploy

      - name: Load PGP signing key and passphrase
        run: |
          mkdir -p ~/.gnupg
          echo "default-cache-ttl 7200" >> ~/.gnupg/gpg-agent.conf

          # Set the appropriate permissions for files and directories.
          find ~/.gnupg -type f -exec chmod 600 {} \;
          find ~/.gnupg -type d -exec chmod 700 {} \;

          # Test the agent configuration.  This is helpful for debugging if
          # something is invalid.
          gpgconf --check-options gpg-agent
          gpg-agent --gpgconf-test

          # Kill the agent, if one is running, and then start a new one - this
          # ensures our updated configuration is applied correctly.
          gpgconf --kill gpg-agent
          gpgconf --launch gpg-agent

          # Retrieve our signing key and import it into gpg.
          gcloud --project astral-field-294621 secrets versions access latest --secret=linux-releases-pgp-key | gpg --batch --import

          # Populate the passphrase for our signing key in the gpg-agent cache.
          # by signing an empty file.
          touch emptyfile
          gpg --no-tty --pinentry-mode loopback --passphrase-fd 0 --detach-sign --armor --batch --yes --output emptyfile.sig emptyfile << EOF
          ${{ secrets.LINUX_RELEASES_PGP_KEY_PASSPHRASE }}
          EOF
          rm emptyfile.sig
        shell: bash

      # Namespace's User Bundled Cache persists target/ between jobs on the same profile, which can
      # leave stale packages from a previous channel's build in the linux bundle output dir.
      - name: Clean stale bundle output
        run: |
          if [[ -d target ]]; then
            find target -path '*/bundle/linux' -type d -prune -exec rm -rf {} +
          fi
        shell: bash

      - name: Bundle app
        id: bundle_app
        run: |
          # Build everything but the arch package, as we need to build it
          # within a Docker container.
          script/bundle --channel $CHANNEL --packages appimage,deb,rpm
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}

      - name: Archive canonical Linux settings schema
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: settings-schema-linux-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          path: ${{ steps.bundle_app.outputs.settings_schema_path }}

      - name: Bundle Arch Linux package
        uses: ./.github/actions/bundle_arch_package
        with:
          channel: ${{ steps.get-config.outputs.channel }}
          release-tag: ${{ needs.prepare_release.outputs.release_tag }}
          arch: x86_64
          settings-schema-source: ${{ steps.bundle_app.outputs.settings_schema_path }}

      - name: Sign Arch Linux packages
        run: |
          ./script/linux/sign_arch_packages "$PKGDIR"
        shell: bash
        env:
          PKGDIR: ${{ steps.bundle_app.outputs.packages_dir }}

      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      - name: Upload debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        shell: bash
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_app.outputs.debug_executable_path }}

      - name: Add packages to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        id: upload_github_assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_app.outputs.packages_dir }}/*
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload packages to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_app.outputs.packages_dir }}
          # Only upload the files from within `packages_dir`, don't include the
          # (parent) folder itself.
          parent: false
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
          # IMPORTANT: both the key and value of the cache-control header must be all lowercase;
          # uppercase values will not be respected by Cloud CDN and using "Cache-Control" will be
          # ignored by the upload-cloud-storage action.
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload packages as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-linux-x86_64-${{ steps.get-config.outputs.channel }}
          path: ${{ steps.bundle_app.outputs.packages_dir }}

  build_linux_cli_x86_binaries:
    name: Build Release Binary (Linux CLI x86_64)
    runs-on: namespace-profile-ubuntu-22-04
    needs: prepare_release
    if: ${{ inputs.build_linux != false }}
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: linux
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      # Install gcc 10, as gcc 9.5 has a bug with memcmp that is incompatible
      # with aws-lc-rs (see https://gcc.gnu.org/bugzilla/show_bug.cgi?id=95189).
      - name: Install and configure gcc-10
        run: |
          sudo apt update
          sudo apt install -y gcc-10 g++-10
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          gcc --version
          g++ --version

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      # Remove cached _bundle_ output that can be shared across different _jobs_.  This won't
      # remove any incremental build artifacts, only the final bundled output.
      - name: Clean stale bundle output
        run: |
          if [[ -d target ]]; then
            find target -path '*/bundle/linux' -type d -prune -exec rm -rf {} +
          fi
        shell: bash

      # Only the binary matters here; this job's own bundled resources are
      # discarded, so skip the (otherwise mandatory) schema and third-party
      # license generation, both of which belong to the packaging step below.
      # --packages none avoids building the (app-only) AppImage that
      # --packages otherwise defaults to.
      - name: Build CLI binary
        id: build_cli
        run: script/bundle --channel "$CHANNEL" --artifact cli --packages none
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          NO_LICENSES: "1"
          SKIP_SETTINGS_SCHEMA: "1"

      # Unlike the TUI job above, the cli artifact doesn't stage a copy into
      # bundle/linux, so executable_path and debug_executable_path are
      # already the raw target/<profile>/<bin>[.debug] Cargo output paths;
      # no derivation is needed to satisfy --skip-build below.
      - name: Create tar archive with build artifacts
        run: |
          EXECUTABLE_PATH="$(realpath --relative-to=$(pwd) "$EXECUTABLE_PATH")"
          DEBUG_EXECUTABLE_PATH="$(realpath --relative-to=$(pwd) "$DEBUG_EXECUTABLE_PATH")"
          tar czvf build-artifacts.tar.gz "$EXECUTABLE_PATH" "$DEBUG_EXECUTABLE_PATH"
        shell: bash
        env:
          EXECUTABLE_PATH: ${{ steps.build_cli.outputs.executable_path }}
          DEBUG_EXECUTABLE_PATH: ${{ steps.build_cli.outputs.debug_executable_path }}

      - name: Archive Linux CLI build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linux-cli-x86_64-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          path: build-artifacts.tar.gz

  # Packages the CLI binary build_linux_cli_x86_binaries built separately (in
  # parallel), then adds the canonical schema.
  release_linux_cli_x86:
    name: Build Release (Linux CLI x86_64)
    runs-on: namespace-profile-ubuntu-22-04
    needs: [prepare_release, release_linux_x86, build_linux_cli_x86_binaries]
    if: ${{ inputs.build_linux != false }}
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: linux
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      # Install gcc 10, as gcc 9.5 has a bug with memcmp that is incompatible
      # with aws-lc-rs (see https://gcc.gnu.org/bugzilla/show_bug.cgi?id=95189).
      - name: Install and configure gcc-10
        run: |
          sudo apt update
          sudo apt install -y gcc-10 g++-10
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          gcc --version
          g++ --version

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Load PGP signing key and passphrase
        run: |
          mkdir -p ~/.gnupg
          echo "default-cache-ttl 7200" >> ~/.gnupg/gpg-agent.conf

          # Set the appropriate permissions for files and directories.
          find ~/.gnupg -type f -exec chmod 600 {} \;
          find ~/.gnupg -type d -exec chmod 700 {} \;

          # Test the agent configuration.  This is helpful for debugging if
          # something is invalid.
          gpgconf --check-options gpg-agent
          gpg-agent --gpgconf-test

          # Kill the agent, if one is running, and then start a new one - this
          # ensures our updated configuration is applied correctly.
          gpgconf --kill gpg-agent
          gpgconf --launch gpg-agent

          # Retrieve our signing key and import it into gpg.
          gcloud --project astral-field-294621 secrets versions access latest --secret=linux-releases-pgp-key | gpg --batch --import

          # Populate the passphrase for our signing key in the gpg-agent cache.
          # by signing an empty file.
          touch emptyfile
          gpg --no-tty --pinentry-mode loopback --passphrase-fd 0 --detach-sign --armor --batch --yes --output emptyfile.sig emptyfile << EOF
          ${{ secrets.LINUX_RELEASES_PGP_KEY_PASSPHRASE }}
          EOF
          rm emptyfile.sig
        shell: bash

      # Namespace's User Bundled Cache persists target/ between jobs on the same profile, which can
      # leave stale packages from a previous channel's build in the linux bundle output dir.
      - name: Clean stale bundle output
        run: |
          if [[ -d target ]]; then
            find target -path '*/bundle/linux' -type d -prune -exec rm -rf {} +
          fi
        shell: bash

      - name: Download Linux CLI build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: linux-cli-x86_64-${{ steps.get-config.outputs.channel }}

      - name: Extract Linux CLI build artifacts
        run: tar xzvf build-artifacts.tar.gz
        shell: bash

      - name: Download canonical Linux settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-linux-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-linux

      - name: Bundle CLI
        id: bundle_cli
        run: |
          # Package the already-built CLI binary
          script/bundle --channel $CHANNEL --skip-build --artifact cli --packages deb,rpm
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-linux/settings_schema.json

      - name: Package CLI tar.gz
        run: |
          cp ${{ steps.bundle_cli.outputs.executable_path }} oz-${{ steps.get-config.outputs.channel }}
          tar czf oz-${{ steps.get-config.outputs.channel }}-linux-x86_64.tar.gz oz-${{ steps.get-config.outputs.channel }} -C "$(dirname "${{ steps.bundle_cli.outputs.bundled_resources_dir }}")" resources

      - name: Bundle Arch Linux CLI package
        uses: ./.github/actions/bundle_arch_package
        with:
          channel: ${{ steps.get-config.outputs.channel }}
          release-tag: ${{ needs.prepare_release.outputs.release_tag }}
          arch: x86_64
          artifact: cli
          settings-schema-source: .release-artifacts/settings-schema-linux/settings_schema.json

      - name: Sign Arch Linux packages
        run: |
          ./script/linux/sign_arch_packages "$PKGDIR"
        shell: bash
        env:
          PKGDIR: ${{ steps.bundle_cli.outputs.packages_dir }}

      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      - name: Upload debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        shell: bash
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_cli.outputs.debug_executable_path }}

      - name: Add CLI packages to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        id: upload_github_assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_cli.outputs.packages_dir }}/*
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload CLI packages to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_cli.outputs.packages_dir }}
          # Only upload the files from within `packages_dir`, don't include the
          # (parent) folder itself.
          parent: false
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
          # IMPORTANT: both the key and value of the cache-control header must be all lowercase;
          # uppercase values will not be respected by Cloud CDN and using "Cache-Control" will be
          # ignored by the upload-cloud-storage action.
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Add CLI tar.gz to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: oz-${{ steps.get-config.outputs.channel }}-linux-x86_64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload CLI tar.gz to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: oz-${{ steps.get-config.outputs.channel }}-linux-x86_64.tar.gz
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}/cli/linux/x86_64
          process_gcloudignore: false
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload CLI packages as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-linux-cli-x86_64-${{ steps.get-config.outputs.channel }}
          path: |
            ${{ steps.bundle_cli.outputs.packages_dir }}
            oz-${{ steps.get-config.outputs.channel }}-linux-x86_64.tar.gz

  build_linux_arm_binaries:
    name: Build Release (Linux ARM)
    runs-on: namespace-profile-ubuntu-20-04-arm
    needs: prepare_release
    if: ${{ inputs.build_linux != false }}
    timeout-minutes: 90
    env:
      # Automatically extract AppImages before running them instead of mounting
      # them with FUSE, which isn't available on GitHub runners (and this is
      # easier and less error-prone than trying to install it).
      APPIMAGE_EXTRACT_AND_RUN: "1"
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: linux
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      # Install gcc 10, as gcc 9.5 has a bug with memcmp that is incompatible
      # with aws-lc-rs (see https://gcc.gnu.org/bugzilla/show_bug.cgi?id=95189).
      - name: Install and configure gcc-10
        run: |
          sudo apt update
          sudo apt install -y gcc-10 g++-10
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          gcc --version
          g++ --version

      - name: Update path
        run: |
          # Exports .local/bin into the path for subsequent runs.
          # We need this for the linuxdeploy install and bundle steps,
          # as the linuxdeploy appimage is installed to $HOME/local/bin.
          if  [[ ":$PATH:" != *":$HOME/.local/bin:"* ]]; then
              echo "Adding $HOME/.local/bin to PATH"
              echo "$HOME/.local/bin" >> $GITHUB_PATH
          fi
        shell: bash

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Install linuxdeploy
        run: script/linux/install_linuxdeploy

      # Namespace's User Bundled Cache persists target/ between jobs on the same profile, which can
      # leave stale packages from a previous channel's build in the linux bundle output dir.
      - name: Clean stale bundle output
        run: rm -rf target/*/bundle/linux
        shell: bash

      - name: Build app
        id: build_app
        run: |
          # Build the code, but only bundle the appimage
          script/bundle --channel $CHANNEL --packages appimage
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}

      - name: Package binaries
        id: package_binaries
        run: |
          # Tar up all the binaries we need for the next stage
          EXECUTABLE_PATH="$(realpath --relative-to=$(pwd) $EXECUTABLE_PATH)"
          DEBUG_EXECUTABLE_PATH="$(realpath --relative-to=$(pwd) $DEBUG_EXECUTABLE_PATH)"
          PACKAGES_DIR="$(realpath --relative-to=$(pwd) $PACKAGES_DIR)"
          cp "$SETTINGS_SCHEMA_PATH" settings_schema.json
          tar czvf binaries.tar.gz $EXECUTABLE_PATH $DEBUG_EXECUTABLE_PATH $PACKAGES_DIR settings_schema.json
        shell: bash
        env:
          EXECUTABLE_PATH: ${{ steps.build_app.outputs.executable_path }}
          DEBUG_EXECUTABLE_PATH: ${{ steps.build_app.outputs.debug_executable_path }}
          PACKAGES_DIR: ${{steps.build_app.outputs.packages_dir}}
          SETTINGS_SCHEMA_PATH: ${{ steps.build_app.outputs.settings_schema_path }}

      - name: Archive build files
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: built-arm64-linux-${{steps.get-config.outputs.channel}}
          retention-days: 1
          path: binaries.tar.gz

  build_linux_cli_arm_binaries:
    name: Build Release (Linux CLI ARM)
    runs-on: namespace-profile-ubuntu-22-04
    needs: prepare_release
    if: ${{ inputs.build_linux != false }}
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: linux
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      # Install gcc 10, as gcc 9.5 has a bug with memcmp that is incompatible
      # with aws-lc-rs (see https://gcc.gnu.org/bugzilla/show_bug.cgi?id=95189).
      - name: Install and configure gcc-10
        run: |
          sudo apt update
          sudo apt install -y gcc-10 g++-10
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          gcc --version
          g++ --version

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      # Namespace's User Bundled Cache persists target/ between jobs on the same profile, which can
      # leave stale packages from a previous channel's build in the linux bundle output dir.
      - name: Clean stale bundle output
        run: rm -rf target/*/bundle/linux
        shell: bash

      # This cross-compiles aarch64 on an x86_64 host, so the just-built binary
      # cannot be executed here to generate a schema. release_linux_arm, which
      # packages this binary, supplies the canonical SETTINGS_SCHEMA_SOURCE
      # itself, so this job never needs one.
      - name: Build CLI
        id: build_cli
        run: |
          # Build the CLI only
          script/bundle --channel $CHANNEL --artifact cli --packages none --arch aarch64
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          SKIP_SETTINGS_SCHEMA: "1"

      - name: Package binaries
        id: package_binaries
        run: |
          # Tar up all the binaries we need for the next stage
          EXECUTABLE_PATH="$(realpath --relative-to=$(pwd) $EXECUTABLE_PATH)"
          DEBUG_EXECUTABLE_PATH="$(realpath --relative-to=$(pwd) $DEBUG_EXECUTABLE_PATH)"
          PACKAGES_DIR="$(realpath --relative-to=$(pwd) $PACKAGES_DIR)"
          tar czvf binaries-cli.tar.gz $EXECUTABLE_PATH $DEBUG_EXECUTABLE_PATH $PACKAGES_DIR
        shell: bash
        env:
          EXECUTABLE_PATH: ${{ steps.build_cli.outputs.executable_path }}
          DEBUG_EXECUTABLE_PATH: ${{ steps.build_cli.outputs.debug_executable_path }}
          PACKAGES_DIR: ${{steps.build_cli.outputs.packages_dir}}

      - name: Archive build files
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: built-arm64-linux-cli-${{steps.get-config.outputs.channel}}
          retention-days: 1
          path: binaries-cli.tar.gz

  # This is done as a separate job from `build_linux_arm_binaries` and `build_linux_cli_arm_binaries`
  # because the steps here were hard to get running on the self-hosted runners.
  release_linux_arm:
    name: Bundle Release (Linux ARM)
    runs-on: namespace-profile-ubuntu-20-04
    needs: [ prepare_release, release_linux_x86, build_linux_arm_binaries, build_linux_cli_arm_binaries ]
    if: ${{ inputs.build_linux != false }}
    timeout-minutes: 60
    steps:
      - name: Checkout sources
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: ${{ needs.prepare_release.outputs.release_branch }}

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Download canonical Linux settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-linux-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-linux

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
      
      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      # Install gcc 10, as gcc 9.5 has a bug with memcmp that is incompatible
      # with aws-lc-rs (see https://gcc.gnu.org/bugzilla/show_bug.cgi?id=95189).
      - name: Install and configure gcc-10
        run: |
          sudo apt update
          sudo apt install -y gcc-10 g++-10
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 60 --slave /usr/bin/g++ g++ /usr/bin/g++-10
          gcc --version
          g++ --version

      # Install Linux build dependencies required by cargo-about while preparing
      # the bundled third-party license resources.
      - name: Install Linux build dependencies
        shell: bash
        run: ./script/linux/install_build_deps

      - name: Install release dependencies
        shell: bash
        run: |
          ./script/install_cargo_release_deps --no-build-deps

      - name: Load PGP signing key and passphrase
        run: |
          mkdir -p ~/.gnupg
          echo "default-cache-ttl 7200" >> ~/.gnupg/gpg-agent.conf

          # Set the appropriate permissions for files and directories.
          find ~/.gnupg -type f -exec chmod 600 {} \;
          find ~/.gnupg -type d -exec chmod 700 {} \;

          # Test the agent configuration.  This is helpful for debugging if
          # something is invalid.
          gpgconf --check-options gpg-agent
          gpg-agent --gpgconf-test

          # Kill the agent, if one is running, and then start a new one - this
          # ensures our updated configuration is applied correctly.
          gpgconf --kill gpg-agent
          gpgconf --launch gpg-agent

          # Retrieve our signing key and import it into gpg.
          gcloud --project astral-field-294621 secrets versions access latest --secret=linux-releases-pgp-key | gpg --batch --import
      
          # Populate the passphrase for our signing key in the gpg-agent cache.
          # by signing an empty file.
          touch emptyfile
          gpg --no-tty --pinentry-mode loopback --passphrase-fd 0 --detach-sign --armor --batch --yes --output emptyfile.sig emptyfile << EOF
          ${{ secrets.LINUX_RELEASES_PGP_KEY_PASSPHRASE }}
          EOF
          rm emptyfile.sig
          echo "Populated gpg-agent cache"
        shell: bash
      
      # Bundle the app packages

      # Namespace's User Bundled Cache persists target/ between jobs on the same profile, which can
      # leave stale packages from a previous channel's build in the linux bundle output dir.
      - name: Clean stale bundle output
        run: rm -rf target/*/bundle/linux
        shell: bash

      - name: Download app build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: built-arm64-linux-${{steps.get-config.outputs.channel}}

      - name: Unpackage app binaries
        id: unpackage_app_binaries
        run: |
          tar xzvf binaries.tar.gz
        shell: bash

      - name: Verify Linux settings schemas match
        run: cmp .release-artifacts/settings-schema-linux/settings_schema.json settings_schema.json
        shell: bash

      - name: Bundle deb/rpm app packages
        id: bundle_app
        run: |
          # Bundle everything but the arch package, as we need to bundle it
          # within a Docker container.
          script/bundle --channel $CHANNEL --skip-build --packages deb,rpm --arch aarch64
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-linux/settings_schema.json

      - name: Bundle Arch Linux app package
        uses: ./.github/actions/bundle_arch_package
        with:
          channel: ${{ steps.get-config.outputs.channel }}
          release-tag: ${{ needs.prepare_release.outputs.release_tag }}
          arch: aarch64
          settings-schema-source: .release-artifacts/settings-schema-linux/settings_schema.json
          
      - name: Upload app debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        # Important: we have to do this before the binary is overwritten by the CLI version, which has the same filename
        run: |
          script/sentry_upload_dif.sh
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_app.outputs.debug_executable_path }}
        shell: bash

      # Bundle the CLI packages

      - name: Download CLI build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: built-arm64-linux-cli-${{steps.get-config.outputs.channel}}

      - name: Unpackage CLI binaries
        id: unpackage_cli_binaries
        run: |
          tar xzvf binaries-cli.tar.gz
        shell: bash

      - name: Bundle deb/rpm CLI packages
        id: bundle_cli
        run: |
          script/bundle --channel $CHANNEL --skip-build --arch aarch64 --packages deb,rpm --artifact cli
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-linux/settings_schema.json

      - name: Package CLI tar.gz
        run: |
          cp ${{ steps.bundle_cli.outputs.executable_path }} oz-${{ steps.get-config.outputs.channel }}
          tar czf oz-${{ steps.get-config.outputs.channel }}-linux-aarch64.tar.gz oz-${{ steps.get-config.outputs.channel }} -C "$(dirname "${{ steps.bundle_cli.outputs.bundled_resources_dir }}")" resources

      - name: Bundle Arch Linux CLI package
        uses: ./.github/actions/bundle_arch_package
        with:
          channel: ${{ steps.get-config.outputs.channel }}
          release-tag: ${{ needs.prepare_release.outputs.release_tag }}
          arch: aarch64
          artifact: cli
          settings-schema-source: .release-artifacts/settings-schema-linux/settings_schema.json

      - name: Upload CLI debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_cli.outputs.debug_executable_path }}
        shell: bash
      
      # Common uploading steps

      - name: Sign Arch Linux packages
        run: |
          # We technically only need to run `sign_arch_packages` once, as $APP_PKGDIR and $CLI_PKGDIR should be the same.
          ./script/linux/sign_arch_packages "$APP_PKGDIR"
          ./script/linux/sign_arch_packages "$CLI_PKGDIR"
        shell: bash
        env:
          APP_PKGDIR: ${{ steps.bundle_app.outputs.packages_dir }}
          CLI_PKGDIR: ${{ steps.bundle_cli.outputs.packages_dir }}

      - name: Add app packages to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_app.outputs.packages_dir }}/*
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Add CLI packages to GitHub release assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' && steps.bundle_cli.outputs.packages_dir != steps.bundle_app.outputs.packages_dir }}
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_cli.outputs.packages_dir }}/*
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Add CLI tar.gz to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: oz-${{ steps.get-config.outputs.channel }}-linux-aarch64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload app packages to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_app.outputs.packages_dir }}
          parent: false
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
          # IMPORTANT: both the key and value of the cache-control header must be all lowercase;
          # uppercase values will not be respected by Cloud CDN and using "Cache-Control" will be
          # ignored by the upload-cloud-storage action.
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload CLI packages to Google Cloud Storage
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' && steps.bundle_cli.outputs.packages_dir != steps.bundle_app.outputs.packages_dir }}
        with:
          path: ${{ steps.bundle_cli.outputs.packages_dir }}
          parent: false
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
          # IMPORTANT: both the key and value of the cache-control header must be all lowercase;
          # uppercase values will not be respected by Cloud CDN and using "Cache-Control" will be
          # ignored by the upload-cloud-storage action.
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload CLI tar.gz to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: oz-${{ steps.get-config.outputs.channel }}-linux-aarch64.tar.gz
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}/cli/linux/aarch64
          process_gcloudignore: false
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload packages as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-linux-arm64-${{ steps.get-config.outputs.channel }}
          path: |
            ${{ steps.bundle_app.outputs.packages_dir }}
            oz-${{ steps.get-config.outputs.channel }}-linux-aarch64.tar.gz

  release_web:
    name: Build Release (Web)
    runs-on: namespace-profile-ubuntu-22-04
    needs: prepare_release
    if: ${{ inputs.build_web != false }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: wasm
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Bundle app
        id: bundle_app
        run: |
          script/wasm/bundle --channel $CHANNEL
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}

      - name: Brotli compress app bundle
        run: |
          # The brotli binary won't let us compress in-place, so we need to rename back to the
          # original afterwards.
          brotli --rm "${{ steps.bundle_app.outputs.packages_dir }}"/*
          for file in "${{ steps.bundle_app.outputs.packages_dir }}"/*.br; do
              mv -- "$file" "${file%.br}"
          done
        shell: bash

      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      - name: Upload debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        shell: bash
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_app.outputs.debug_executable_path }}

      - name: Add packages to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        id: upload_github_assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_app.outputs.packages_dir }}/*
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload packages to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_app.outputs.packages_dir }}
          # Only upload the files from within `packages_dir`, don't include the
          # (parent) folder itself.
          gzip: false
          parent: false
          destination: ${{ steps.get-config.outputs.web_gcs_bucket_prefix }}-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
          # IMPORTANT: both the key and value of the cache-control header must be all lowercase;
          # uppercase values will not be respected by Cloud CDN and using "Cache-Control" will be
          # ignored by the upload-cloud-storage action.
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}
            content-encoding: br

      - name: Upload assets to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_app.outputs.assets_dir }}
          parent: false
          # All asset filenames contain the file hash for cashing purposes, so we store them all in
          # one directory so they can be cached across releases.
          destination: ${{ steps.get-config.outputs.web_gcs_bucket_prefix }}-static-assets/
          process_gcloudignore: false
          # IMPORTANT: both the key and value of the cache-control header must be all lowercase;
          # uppercase values will not be respected by Cloud CDN and using "Cache-Control" will be
          # ignored by the upload-cloud-storage action.
          # These assets are all stored under cache busting names, so we want to cache them
          # effectively forever. There's no defined maximum for the max-age directive, but a similar
          # directive suggests a maximum of one year, so that's what's chosen here.
          headers: |-
            cache-control: private, max-age=31536000, immutable

      - name: Upload web bundle as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-web-${{ steps.get-config.outputs.channel }}
          path: ${{ steps.bundle_app.outputs.packages_dir }}

  build_windows_tui_binaries:
    name: Build Release Binary (Windows TUI ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: prepare_release
    if: ${{ inputs.build_windows != false }}
    timeout-minutes: 150
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runner: windows-latest-large
          - arch: arm64
            runner: windows-arm-latest-large
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: windows
          cache_key: tui-${{ matrix.arch }}
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Build Warp Agent CLI
        id: build_tui
        run: script/bundle --artifact tui --channel "$CHANNEL" --skip_build_installer --arch ${{ matrix.arch }}
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}

      # actions/upload-artifact stores paths relative to the least common
      # ancestor of everything it's given; binary_path and pdb_file_path live
      # in the same directory, so uploading them directly would collapse the
      # LCA to that directory and strip the target/<triple>/<profile> prefix
      # that release_windows_tui's own script/bundle invocation expects to
      # find them at. Tar with explicit relative paths instead, so the
      # structure is guaranteed rather than an emergent property of which
      # paths happen to be listed.
      - name: Create tar archive with build artifacts
        run: |
          BINARY_PATH="$(realpath --relative-to="$(pwd)" "$(cygpath -u "$BINARY_PATH")")"
          PDB_FILE_PATH="$(realpath --relative-to="$(pwd)" "$(cygpath -u "$PDB_FILE_PATH")")"
          tar czfv build-artifacts.tar.gz "$BINARY_PATH" "$PDB_FILE_PATH"
        shell: bash
        env:
          BINARY_PATH: ${{ steps.build_tui.outputs.binary_path }}
          PDB_FILE_PATH: ${{ steps.build_tui.outputs.pdb_file_path }}

      - name: Archive Windows TUI binary build
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: windows-tui-binary-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          # We compress the tar archive, so no need to re-compress.
          compression-level: 0
          path: build-artifacts.tar.gz

  # Packages the TUI binary build_windows_tui_binaries built separately (in
  # parallel), then adds the canonical schema and signs it.
  release_windows_tui:
    name: Build Release (Windows TUI ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: [prepare_release, build_windows_binaries, build_windows_tui_binaries]
    if: ${{ inputs.build_windows != false }}
    timeout-minutes: 150
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            release_arch: x86_64
            runner: windows-latest-large
          - arch: arm64
            release_arch: aarch64
            runner: windows-arm-latest-large
    env:
      TRUSTED_SIGNING_ENDPOINT: https://eus.codesigning.azure.net/
      TRUSTED_SIGNING_ACCOUNT: warpdotdev
      TRUSTED_SIGNING_CERT_PROFILE: warpterminal
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: windows
          cache_key: tui-${{ matrix.arch }}
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Download Windows TUI binary build
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: windows-tui-binary-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}

      - name: Extract Windows TUI binary build
        run: tar xzvf build-artifacts.tar.gz
        shell: bash

      - name: Download canonical Windows settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-windows-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-windows

      - name: Test Windows TUI installer
        if: ${{ matrix.arch == 'x64' }}
        run: ./script/windows/test_tui_installer.ps1
        shell: pwsh

      - name: Locate TUI binary build outputs
        id: build_tui
        run: script/bundle --artifact tui --channel "$CHANNEL" --skip_build_binary --skip_build_installer --arch ${{ matrix.arch }}
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}

      - name: Sign Warp Agent CLI and PTY executables
        uses: azure/artifact-signing-action@b443cf8ea4124818d2ea9f043cba29fc3ec47b16 # v1.2.0
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ env.TRUSTED_SIGNING_ENDPOINT }}
          trusted-signing-account-name: ${{ env.TRUSTED_SIGNING_ACCOUNT }}
          certificate-profile-name: ${{ env.TRUSTED_SIGNING_CERT_PROFILE }}
          files: ${{ steps.build_tui.outputs.binary_path }}
          files-folder: ${{ github.workspace }}\app\assets\windows\${{ matrix.arch }}
          files-folder-filter: dll,exe
          files-folder-recurse: true
          files-folder-depth: 2
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256
          # The action's dependency cache is not keyed by runner architecture, so x64 and arm64
          # legs of this matrix can clobber each other's cached tools when they run concurrently
          # (Azure/artifact-signing-action#146).
          cache-dependencies: false
      - name: Build sign-tool command for Inno Setup
        id: setup_signing
        shell: pwsh
        run: ./script/windows/build_inno_sign_tool_command.ps1

      - name: Bundle signed Warp Agent CLI installer
        id: bundle_tui
        run: script/bundle --artifact tui --channel "$CHANNEL" --skip_build_binary --require_signatures --arch ${{ matrix.arch }}
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          SIGN_TOOL_CMD: ${{ steps.setup_signing.outputs.sign_tool_cmd }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-windows/settings_schema.json

      - name: Add TUI installer to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_tui.outputs.installer_path }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload TUI installer to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_tui.outputs.installer_path }}
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}/tui/windows/${{ matrix.release_arch }}
          process_gcloudignore: false
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Verify TUI installer in Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          installer_name="$(basename "$INSTALLER_PATH")"
          curl.exe --fail --silent --show-error --head \
            "https://releases.warp.dev/${CHANNEL}/${RELEASE_TAG}/tui/windows/${RELEASE_ARCH}/${installer_name}"
        shell: bash
        env:
          INSTALLER_PATH: ${{ steps.bundle_tui.outputs.installer_path }}
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          RELEASE_ARCH: ${{ matrix.release_arch }}

      - name: Upload Windows TUI installer
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: release-windows-tui-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          path: |
            ${{ steps.bundle_tui.outputs.installer_path }}
            ${{ steps.bundle_tui.outputs.pdb_file_path }}
          if-no-files-found: error

  build_windows_binaries:
    name: Build Release Binary (Windows ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: prepare_release
    if: ${{ inputs.build_windows != false }}
    timeout-minutes: 150
    strategy:
      matrix:
        include:
          - arch: x64
            runner: windows-latest-large
          - arch: arm64
            runner: windows-arm-latest-large
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: windows
          cache_key: ${{ matrix.arch }}
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Build binary
        id: build_binary
        run: script/bundle -Channel $CHANNEL -skip_build_installer --arch ${{ matrix.arch }}
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}

      - name: Generate canonical Windows settings schema
        if: ${{ matrix.arch == 'x64' }}
        shell: pwsh
        run: |
          $Process = Start-Process `
            -FilePath "${{ steps.build_binary.outputs.binary_path }}" `
            -ArgumentList @('dump-settings-schema', "${{ runner.temp }}\settings_schema.json") `
            -NoNewWindow `
            -PassThru `
            -Wait
          if ($Process.ExitCode -ne 0) { throw "Failed to generate the Windows settings schema: exit code $($Process.ExitCode)." }

      - name: Archive canonical Windows settings schema
        if: ${{ matrix.arch == 'x64' }}
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: settings-schema-windows-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          path: ${{ runner.temp }}\settings_schema.json

      - name: Archive Windows binary build
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: windows-binary-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          retention-days: 1
          path: |
            ${{ steps.build_binary.outputs.binary_path }}
            ${{ steps.build_binary.outputs.pdb_file_path }}
            app/assets/windows/${{ matrix.arch }}

  release_windows:
    name: Bundle Release (Windows ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: [prepare_release, build_windows_binaries]
    if: ${{ inputs.build_windows != false }}
    timeout-minutes: 150
    strategy:
      matrix:
        include:
          - arch: x64
            runner: windows-latest-large
          - arch: arm64
            runner: windows-arm-latest-large
    env:
      TRUSTED_SIGNING_ENDPOINT: https://eus.codesigning.azure.net/
      TRUSTED_SIGNING_ACCOUNT: warpdotdev
      TRUSTED_SIGNING_CERT_PROFILE: warpterminal
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/prepare_environment
        with:
          target_os: windows
          cache_key: ${{ matrix.arch }}
          is_self_hosted: false
          ref: ${{ needs.prepare_release.outputs.release_branch }}
          install_release_deps: true
          ssh_key: ${{ secrets.WARP_CHANNEL_CONFIG_ACCESS_SSH_KEY }}

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      - name: Download Windows binary build
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: windows-binary-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          path: .

      - name: Download canonical Windows settings schema
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: settings-schema-windows-${{ steps.get-config.outputs.channel }}
          path: .release-artifacts/settings-schema-windows

      - name: Locate binary build outputs
        id: binary_paths
        run: script/bundle -Channel $CHANNEL -skip_build_binary -skip_build_installer --arch ${{ matrix.arch }}
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}

      - name: Sign the executables with Azure Trusted Signing
        uses: azure/artifact-signing-action@b443cf8ea4124818d2ea9f043cba29fc3ec47b16 # v1.2.0
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ env.TRUSTED_SIGNING_ENDPOINT }}
          trusted-signing-account-name: ${{ env.TRUSTED_SIGNING_ACCOUNT }}
          certificate-profile-name: ${{ env.TRUSTED_SIGNING_CERT_PROFILE }}
          files: |
            ${{ steps.binary_paths.outputs.binary_path }}
            ${{ github.workspace }}\app\assets\bundled\bootstrap\pwsh.ps1
          files-folder: ${{ github.workspace }}\app\assets\windows\${{ matrix.arch }}
          files-folder-filter: dll,exe
          files-folder-recurse: true
          files-folder-depth: 2
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256
          # The action's dependency cache is not keyed by runner architecture, so x64 and arm64
          # legs of this matrix can clobber each other's cached tools when they run concurrently
          # (Azure/artifact-signing-action#146).
          cache-dependencies: false

      # The azure/artifact-signing-action above downloads signtool.exe and the Trusted Signing dlib.
      # We locate them so ISCC can call signtool during compilation to sign the inner setup engine
      # (the file extracted to %TEMP% at runtime). This prevents Defender ASR rule D4F940AB from
      # blocking the installer in enterprise environments. See this issue:
      # https://github.com/warpdotdev/Warp/issues/8794
      - name: Build sign-tool command for Inno Setup
        id: setup_signing
        shell: pwsh
        run: ./script/windows/build_inno_sign_tool_command.ps1

      - name: Bundle app
        id: bundle_app
        run: script/bundle -Channel $CHANNEL -skip_build_binary --arch ${{ matrix.arch }}
        shell: bash
        env:
          CHANNEL: ${{ steps.get-config.outputs.channel }}
          GIT_RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          SIGN_TOOL_CMD: ${{ steps.setup_signing.outputs.sign_tool_cmd }}
          SETTINGS_SCHEMA_SOURCE: ${{ github.workspace }}/.release-artifacts/settings-schema-windows/settings_schema.json

      # This runner has no upstream match: matbour/setup-sentry-cli has no win32/arm64 entry
      # in its platform table, so it fails with "Unsupported platform: win32/arm64" here. The
      # fork adds that entry (submitted upstream as matbour/setup-sentry-cli#24, not yet in a
      # release).
      - name: Set up Sentry CLI
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: warpdotdev/setup-sentry-cli@8ae49c71bd731e547de697f75770727f86c95ecd # v2.0.0
        with:
          token: ${{ secrets.SENTRY_RELEASE_TOKEN }}
          organization: warpdotdev
          project: ${{ steps.get-config.outputs.sentry_project }}

      - name: Upload binary to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        shell: bash
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.binary_paths.outputs.binary_path }}

      - name: Upload debugging symbols to Sentry
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        run: |
          script/sentry_upload_dif.sh
        shell: bash
        env:
          DEBUG_FILE_OR_FOLDER_PATH: ${{ steps.bundle_app.outputs.pdb_file_path }}

      - name: Add installer to GitHub release assets
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        id: upload_github_assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare_release.outputs.release_tag }}
          files: ${{ steps.bundle_app.outputs.installer_path }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload installer to Google Cloud Storage
        if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: ${{ steps.bundle_app.outputs.installer_path }}
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
          # IMPORTANT: both the key and value of the cache-control header must be all lowercase;
          # uppercase values will not be respected by Cloud CDN and using "Cache-Control" will be
          # ignored by the upload-cloud-storage action.
          headers: |-
            cache-control: ${{ steps.get-config.outputs.gcs_cache_control_value }}

      - name: Upload installer as workflow artifact
        if: ${{ needs.prepare_release.outputs.should_publish != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-windows-${{ matrix.arch }}-${{ steps.get-config.outputs.channel }}
          path: ${{ steps.bundle_app.outputs.installer_path }}

  # Aggregates the result of every build/release job into a single place, so
  # downstream jobs don't each need their own copy of the same "which
  # platforms actually matter, and did they succeed" logic. A platform whose
  # build was deliberately disabled via a workflow input (build_macos,
  # build_linux, build_windows, build_web, or web-on-preview) is excluded
  # entirely, rather than being treated as a failure. Anything else that
  # isn't a plain `success` -- a genuine failure, a cancellation, or a job
  # skipped because one of ITS OWN dependencies failed (e.g. a build-only
  # job) -- counts as a failure. `if: always()` is required so this job
  # still runs, and still reports failures, even when one of the jobs it
  # depends on did not succeed.
  collect_results:
    name: Collect release job results
    runs-on: ubuntu-latest
    needs:
      - prepare_release
      - release_macos_single_arch
      - release_macos_universal
      - release_macos_cli
      - release_macos_tui
      - release_linux_tui
      - release_linux_x86
      - release_linux_arm
      - release_linux_cli_x86
      - release_web
      - release_windows_tui
      - release_windows
    if: always()
    outputs:
      failed: ${{ steps.collect.outputs.failed }}
    steps:
      # This job intentionally fails (after recording `failed` as an output)
      # when any non-disabled platform did not succeed. That failure is the
      # publish gate: generate_changelogs needs this job with default
      # (success-required) semantics, so it is skipped whenever this step
      # fails, exactly like any other failed dependency. Jobs that only need
      # to observe the failure list regardless of outcome (e.g. the Slack
      # notification below) declare `if: always()` on themselves instead.
      - name: Determine which release jobs genuinely failed
        id: collect
        shell: bash
        env:
          BUILD_MACOS: ${{ inputs.build_macos }}
          BUILD_LINUX: ${{ inputs.build_linux }}
          BUILD_WINDOWS: ${{ inputs.build_windows }}
          BUILD_WEB: ${{ inputs.build_web }}
          CHANNEL: ${{ inputs.channel }}
          MACOS_SINGLE_ARCH_RESULT: ${{ needs.release_macos_single_arch.result }}
          MACOS_UNIVERSAL_RESULT: ${{ needs.release_macos_universal.result }}
          MACOS_CLI_RESULT: ${{ needs.release_macos_cli.result }}
          MACOS_TUI_RESULT: ${{ needs.release_macos_tui.result }}
          LINUX_TUI_RESULT: ${{ needs.release_linux_tui.result }}
          LINUX_X86_RESULT: ${{ needs.release_linux_x86.result }}
          LINUX_ARM_RESULT: ${{ needs.release_linux_arm.result }}
          LINUX_CLI_X86_RESULT: ${{ needs.release_linux_cli_x86.result }}
          WEB_RESULT: ${{ needs.release_web.result }}
          WINDOWS_TUI_RESULT: ${{ needs.release_windows_tui.result }}
          WINDOWS_RESULT: ${{ needs.release_windows.result }}
        run: |
          FAILED=()

          # A disabled platform is excluded outright, regardless of its
          # result (which will be "skipped" anyway, since that's what a
          # false job-level `if:` produces). Otherwise, anything other than
          # "success" -- including "skipped", which for a non-disabled
          # platform only happens because one of its own dependencies
          # failed -- is a genuine failure.
          check() {
            local name="$1" result="$2" disabled="$3"
            if [[ "$disabled" == "true" ]]; then
              return
            fi
            if [[ "$result" != "success" ]]; then
              FAILED+=("$name")
            fi
          }

          MACOS_DISABLED=false
          [[ "$BUILD_MACOS" == "false" ]] && MACOS_DISABLED=true
          LINUX_DISABLED=false
          [[ "$BUILD_LINUX" == "false" ]] && LINUX_DISABLED=true
          WINDOWS_DISABLED=false
          [[ "$BUILD_WINDOWS" == "false" ]] && WINDOWS_DISABLED=true
          WEB_DISABLED=false
          [[ "$BUILD_WEB" == "false" || "$CHANNEL" == "preview" ]] && WEB_DISABLED=true

          check "MacOS Single Arch" "$MACOS_SINGLE_ARCH_RESULT" "$MACOS_DISABLED"
          check "MacOS Universal" "$MACOS_UNIVERSAL_RESULT" "$MACOS_DISABLED"
          check "MacOS CLI" "$MACOS_CLI_RESULT" "$MACOS_DISABLED"
          check "MacOS TUI" "$MACOS_TUI_RESULT" "$MACOS_DISABLED"
          check "Linux TUI" "$LINUX_TUI_RESULT" "$LINUX_DISABLED"
          check "Linux x86_64" "$LINUX_X86_RESULT" "$LINUX_DISABLED"
          check "Linux ARM64" "$LINUX_ARM_RESULT" "$LINUX_DISABLED"
          check "Linux CLI x86_64" "$LINUX_CLI_X86_RESULT" "$LINUX_DISABLED"
          check "WASM" "$WEB_RESULT" "$WEB_DISABLED"
          check "Windows TUI" "$WINDOWS_TUI_RESULT" "$WINDOWS_DISABLED"
          check "Windows" "$WINDOWS_RESULT" "$WINDOWS_DISABLED"

          if (( ${#FAILED[@]} )); then
            printf -v FORMATTED "%s, " "${FAILED[@]}"
            echo "failed=$(echo "{${FORMATTED%, }}")" >> "$GITHUB_OUTPUT"
            echo "::error::The following release jobs did not succeed: ${FAILED[*]}"
            exit 1
          fi

  send_slack_notif_if_unsuccessful:
    name: Send Slack notification if any release jobs were unsuccessful
    runs-on: ubuntu-latest
    needs:
      - prepare_release
      - collect_results
    if: ${{ always() && needs.prepare_release.outputs.should_publish == 'true' }}
    steps:
      - name: Send Slack notification if unsuccessful
        uses: slackapi/slack-github-action@0d95c9a7becc1e6e297d76df9bc735c44f4cbcbc # v3.0.5
        if: ${{ needs.collect_results.outputs.failed != '' }}
        with:
          # Fail the step when Slack rejects the message.
          errors: true
          method: chat.postMessage
          token: ${{ secrets.ACTION_MONITORING_SLACK }}
          payload: |
            channel: "#oncall-client"
            # Follows https://api.slack.com/reference/surfaces/formatting#mentioning-users.
            # S06N82RS7FA is the user group ID for @oncall-client-eng.
            text: "Failed to create ${{ needs.collect_results.outputs.failed }} release artifacts for ${{ inputs.channel }}. See https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}. cc: <!subteam^S06N82RS7FA>"

  generate_changelogs:
    name: Generate Changelogs
    if: ${{ needs.prepare_release.outputs.should_publish == 'true' }}
    runs-on: ubuntu-latest
    needs:
      - prepare_release
      - collect_results
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        name: Checkout sources
        with:
          # Fetch history for all tags and branches so we can compare revisions
          # in order to generate the changelog.
          fetch-depth: 0

      - name: Get channel configuration
        id: get-config
        uses: ./.github/actions/get_channel_config/
        with:
          config_file: ${{ env.CONFIG_FILE }}
          channel: ${{ inputs.channel }}

      # Stable releases use the Oz changelog-draft agent for higher-quality,
      # human-reviewable output. Non-stable channels (dev/preview/beta) use the
      # legacy generate-changelog action to avoid spending Oz agent tokens on
      # daily dev cuts and preview RCs.
      - name: Generate changelog via Oz (stable only)
        if: inputs.channel == 'stable'
        uses: warpdotdev/oz-agent-action@ce1621abf6a8ed8afdd4e4cc994545ede8fe1c6f # main
        with:
          prompt: |
            Generate a changelog draft for the ${{ inputs.channel }} channel, release tag ${{ needs.prepare_release.outputs.release_tag }}.

            Output directory: ${{ runner.temp }}/changelog-draft

            Follow the workflow in .agents/skills/changelog-draft/SKILL.md exactly.
            Make sure to produce both output files: changelog-draft.md and changelog-draft.json.
            The release workflow may run from warpdotdev/warp-internal. When fetching PR data, pass the checked-out repository ("${{ github.repository }}") to fetch_prs.py and rely on the script's repo-sync normalization to resolve public warpdotdev/warp PR numbers, URLs, and authors. The script intentionally omits non-repo-sync PRs from warp-internal because they are private internal changes. Do not infer or synthesize public PR links manually.

            After writing the output files, print the full contents of changelog-draft.md to stdout so it appears in the workflow log.

            You are running in a GitHub Actions workflow. The repo is checked out at the default branch (HEAD) with full history. Use the release_tag input as the git range endpoint — do NOT check out the release tag. `gh` is authenticated. Do not commit, push, or create PRs.
          warp_api_key: ${{ secrets.WARP_API_KEY }}
          share: team
      - name: Upload raw Markdown changelog artifact (stable only)
        if: inputs.channel == 'stable'
        id: upload_changelog_draft_markdown
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: changelog-draft-markdown
          path: ${{ runner.temp }}/changelog-draft/changelog-draft.md
          if-no-files-found: error

      - name: Convert draft JSON to release format (stable only)
        if: inputs.channel == 'stable'
        shell: bash
        run: |
          python3 .agents/skills/changelog-draft/scripts/convert_to_release_json.py \
            --input "${{ runner.temp }}/changelog-draft/changelog-draft.json" \
            --output "${{ runner.temp }}/changelog-draft/changelog-release.json"

      - name: Obtain a GitHub App Installation Access Token (non-stable only)
        if: inputs.channel != 'stable'
        id: github_app_auth
        run: |
          TOKEN="$(npx obtain-github-app-installation-access-token ci ${{ secrets.GH_APP_CREDENTIALS_TOKEN }})"
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Generate changelog via legacy action (non-stable only)
        if: inputs.channel != 'stable'
        id: legacy_changelog
        uses: warpdotdev/generate-changelog@70f534c1e030dafb45046ae57e4aa4d43a2f5c84 # main
        with:
          channel: ${{ inputs.channel }}
          github_auth_token: ${{ steps.github_app_auth.outputs.token }}
          version: ${{ needs.prepare_release.outputs.release_tag }}
      - name: Add TUI updates to legacy changelog (non-stable only)
        if: inputs.channel != 'stable'
        shell: bash
        env:
          GH_TOKEN: ${{ steps.github_app_auth.outputs.token }}
          LEGACY_CHANGELOG: ${{ steps.legacy_changelog.outputs.changelog }}
        run: |
          CHANGELOG_DIR="${{ runner.temp }}/changelog-draft"
          LEGACY_CHANGELOG_FILE="$CHANGELOG_DIR/changelog-legacy.json"
          mkdir -p "$CHANGELOG_DIR"
          printf '%s\n' "$LEGACY_CHANGELOG" > "$LEGACY_CHANGELOG_FILE"
          python3 .agents/skills/changelog-draft/scripts/add_tui_updates.py \
            --input "$LEGACY_CHANGELOG_FILE" \
            --output "$CHANGELOG_DIR/changelog-release.json" \
            --repo "${{ github.repository }}" \
            --channel "${{ inputs.channel }}" \
            --release-tag "${{ needs.prepare_release.outputs.release_tag }}"

      - name: Load changelog into step output
        id: generate_changelog
        shell: bash
        run: |
          # Both generators now write the same release-format file.
          CHANGELOG_FILE="${{ runner.temp }}/changelog-draft/changelog-release.json"
          if [ ! -f "$CHANGELOG_FILE" ]; then
            echo "::error::changelog-release.json not found at $CHANGELOG_FILE"
            exit 1
          fi
          jq empty "$CHANGELOG_FILE"
          {
            echo "changelog<<CHANGELOG_EOF"
            cat "$CHANGELOG_FILE"
            echo "CHANGELOG_EOF"
          } >> $GITHUB_OUTPUT

      - name: Build Slack changelog payload
        id: build_slack_payload
        shell: bash
        env:
          CHANGELOG_JSON: ${{ steps.generate_changelog.outputs.changelog }}
          RELEASE_TAG: ${{ needs.prepare_release.outputs.release_tag }}
          MARKDOWN_ARTIFACT_URL: ${{ steps.upload_changelog_draft_markdown.outputs.artifact-url }}
          SLACK_CHANNEL: ${{ steps.get-config.outputs.changelog_slack_channel }}
        run: |
          CHANGELOG_INPUT="${{ runner.temp }}/slack-changelog-input.json"
          SLACK_PAYLOAD="${{ runner.temp }}/slack-changelog-payload.json"
          printf '%s\n' "$CHANGELOG_JSON" > "$CHANGELOG_INPUT"
          python3 .agents/skills/changelog-draft/scripts/build_slack_payload.py \
            --input "$CHANGELOG_INPUT" \
            --release-tag "$RELEASE_TAG" \
            --markdown-artifact-url "$MARKDOWN_ARTIFACT_URL" \
            --output "$SLACK_PAYLOAD"
          if ! jq -e '.blocks | length > 0' "$SLACK_PAYLOAD" >/dev/null; then
            echo "has_content=" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "has_content=true" >> $GITHUB_OUTPUT

          # chat.postMessage takes the destination channel as one of its
          # arguments, so fold it into the payload the Slack step sends.
          jq -c --arg channel "$SLACK_CHANNEL" '. + {channel: $channel}' "$SLACK_PAYLOAD" > "$SLACK_PAYLOAD.tmp"
          mv "$SLACK_PAYLOAD.tmp" "$SLACK_PAYLOAD"

          echo "payload<<SLACK_PAYLOAD_EOF" >> $GITHUB_OUTPUT
          cat "$SLACK_PAYLOAD" >> $GITHUB_OUTPUT
          echo "SLACK_PAYLOAD_EOF" >> $GITHUB_OUTPUT

      - name: Post to a Slack channel
        id: slack
        uses: slackapi/slack-github-action@0d95c9a7becc1e6e297d76df9bc735c44f4cbcbc # v3.0.5
        if: ${{ steps.build_slack_payload.outputs.has_content == 'true' }}
        # Continue on with the rest of the workflow, even if we fail to send the
        # changelog info to Slack.  (We still want to stick the changelog JSON
        # in Cloud Storage.)
        continue-on-error: true
        with:
          # Fail the step when Slack rejects the message; continue-on-error
          # above still lets the rest of the job run.
          errors: true
          method: chat.postMessage
          token: ${{ secrets.SLACK_CHANGELOG_BOT }}
          payload: ${{ steps.build_slack_payload.outputs.payload }}

      - name: Generate changelog.json
        id: changelog_json
        env:
          CHANGELOG: ${{ steps.generate_changelog.outputs.changelog }}
        run: |
          # Compute the current date/time in the necessary format.
          DATE=$(date +%Y-%m-%dT%k:%M:%S%z)
          # Rename the "newFeatures" and "improvements" keys in the Changelog JSON to something more human readable
          # and ignore everything else since this is for the in-app changelog.
          CHANGELOG_SECTIONS=$(echo $CHANGELOG | jq 'with_entries(select(.key == "newFeatures" or .key == "improvements"))' | jq 'with_entries(if .key == "newFeatures" then .key = "New features" else . end)' |  jq 'with_entries(if .key == "improvements" then .key = "Improvements" else . end)')
          # create markdown strings for the New Features and Improvements sections
          NEW_FEATURES=$(echo $CHANGELOG_SECTIONS | jq 'with_entries(select(.key == "New features"))' | jq -r 'to_entries[] | "* \(.value[])"' | awk -v ORS='\n' '1')
          IMPROVEMENTS=$(echo $CHANGELOG_SECTIONS | jq 'with_entries(select(.key == "Improvements"))' | jq -r 'to_entries[] | "* \(.value[])"' | awk -v ORS='\n' '1')
          # Extract the image URL from the list, and use the latest URL even if there are multiple
          IMAGE=$(echo $CHANGELOG | jq -r '.images | if length > 0 then .[-1] else "" end')
          # Extract Oz updates as a JSON array
          OZ_UPDATES=$(echo $CHANGELOG | jq -c '.oz_updates // []')
          # Extract TUI updates as a JSON array
          TUI_UPDATES=$(echo $CHANGELOG | jq -c '.tui_updates // []')
          # Tweak the structure of the JSON, add in a top-level date field, and add in the markdown_sections field
          CHANGELOG=$(echo $CHANGELOG_SECTIONS | jq --arg date "$DATE" --arg new_features "$NEW_FEATURES" --arg improvements "$IMPROVEMENTS" --arg image "$IMAGE" --argjson oz_updates "$OZ_UPDATES" --argjson tui_updates "$TUI_UPDATES" '{"date": $date, "sections": to_entries | map({"title": .key, "items": .value}), "markdown_sections": [{"title": "New features", "markdown": $new_features}, {"title": "Improvements", "markdown": $improvements}, {"title": "Coming soon", "markdown": ""}]} + (if $image != "" then {"image_url": $image} else {} end) + (if ($oz_updates | length) > 0 then {"oz_updates": $oz_updates} else {} end) + (if ($tui_updates | length) > 0 then {"tui_updates": $tui_updates} else {} end)')

          echo $CHANGELOG > changelog.json
          cat changelog.json
        shell: bash

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

      - name: Upload changelog.json to Google Cloud Storage
        uses: google-github-actions/upload-cloud-storage@6397bd7208e18d13ba2619ee21b9873edc94427a # v3.0.0
        with:
          path: changelog.json
          destination: warp-releases/${{ steps.get-config.outputs.channel }}/${{ needs.prepare_release.outputs.release_tag }}
          process_gcloudignore: false
